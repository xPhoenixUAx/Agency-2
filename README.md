# SIGNAL — деплой сайту

Сайт лежить у корені проєкту. Завантажте HTML-файли й папки `api/`, `assets/`, `config/`, `css/`, `js/` у корінь сайту на PHP-хостингу. Не завантажуйте `.git/` і цей README.

Сайт працює на HTML/CSS/JavaScript і PHP 8.1+; локально перевірено на PHP 8.3. Збірка, Node.js, Python та встановлення пакетів не потрібні. Редагуйте файли безпосередньо — генераторів немає.

## Структура

- `*.html` — сторінки сайту, юридичні тексти й редактор бренду.
- `css/`, `js/` — стилі та інтерактивність.
- `assets/` — використовувані зображення, SVG і локальний Inter з ліцензією.
- `config/site.json` — бренд, контакти, юридичні реквізити, кольори, CTA та варіанти форми.
- `api/lead.php` — обробник заявок.
- `api/server.php` — серверні параметри пошти.

## Налаштування перед запуском

1. Замініть приклади контактів, домену та реквізитів у `config/site.json`. Редактор `rebrand.html` створює JSON для ручної заміни цього файла.
2. У налаштуваннях PHP-хостингу задайте `AGENCY_MAIL_FROM` — справжню адресу відправника. За потреби задайте `AGENCY_MAIL_TO`; інакше отримувач береться з `brand.email`. Можна також заповнити ці значення в `api/server.php`.
3. Налаштуйте доставку через PHP `mail()`. Потрібні робочі PHP-сесії та доступний для запису системний тимчасовий каталог. На продакшені не встановлюйте `AGENCY_ENV=test` та `AGENCY_TEST_OUTBOX`.
4. Заповніть юридичні параметри нижче й звірте політики з фактичними умовами бізнесу та хостингу. Нинішні кейси позначені як ілюстративні; їх видимість керується `features.showIllustrativeCases`.
5. Перевірте заявку на своєму хостингу. Реальну доставку пошти локальні перевірки не підтверджують.

## Юридичні сторінки та конфіг

Сторінки `privacy.html`, `terms.html` і `cookies.html` містять повні тексти для сайту
агенції з формою заявки. Terms of Use регулюють сайт і запит аудиту; оплату, строки,
припинення послуг та доступ до клієнтських даних визначає окремий договір.
Тексти не обирають країну, суд чи реєстраційні дані за клієнта. Універсальний текст
не гарантує відповідності кожній юрисдикції: після визначення країни та ринку
перевірте місцеві вимоги, зокрема до реквізитів, представника, cookies та прав споживачів.

Усі значення нижче можна змінити в `config/site.json` або через `rebrand.html`.
Публічні тексти вводяться англійською. HTML у значеннях не виконується.

| Поле                                                                          | Призначення                                                                               |
| ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| `brand.name`, `brand.legalName`                                               | Бренд і юридична назва оператора / контролера даних                                       |
| `brand.address`, `brand.email`, `brand.website`                               | Адреса, основна пошта, сайт                                                               |
| `legal.updatedOn`                                                             | Дата редакції всіх трьох політик у форматі YYYY-MM-DD; змінювати при оновленні текстів    |
| `legal.registeredCountry`, `legal.registrationNumber`, `legal.taxNumber`      | Необов’язкові реквізити; порожні рядки приховані                                          |
| `legal.privacyEmail`                                                          | Пошта для запитів щодо даних; порожньо — використовується brand.email                     |
| `legal.privacyContactDetails`                                                 | Контакти DPO або представника, якщо вони потрібні                                         |
| `legal.providers`                                                             | Фактичні хостинг, поштові та інші постачальники / категорії отримувачів                   |
| `legal.internationalTransfers`                                                | Країни обробки, застосовні механізми передачі та спосіб отримання гарантій                |
| `legal.enquiryRetention`, `legal.technicalRetention`, `legal.clientRetention` | Строки або критерії зберігання заявок, технічних записів і клієнтських документів         |
| `legal.governingLaw`                                                          | Необов’язкова погоджена умова про застосовне право, без скасування обов’язкових прав      |
| `legal.supervisoryAuthority`, `legal.supervisoryAuthorityUrl`                 | Назва та URL компетентного органу захисту даних                                           |
| `legal.additionalCookieDetails`                                               | Назви, призначення, постачальники й строки додаткових cookies хостингу / CDN, якщо вони є |
| `links.privacy`, `links.terms`, `links.cookies`                               | Адреси юридичних сторінок                                                                 |

Значення-приклади бренду й контактів потрібно замінити перед публікацією.
Конфіг підставляється JavaScript; без JS основні тексти читаються, але реквізити
залишаються статичними значеннями HTML. Якщо потрібні актуальні реквізити без JS,
підставте їх також у HTML або додайте серверний рендеринг конфігу.

Зміна параметрів зберігання в конфігу змінює текст політики, а не запускає видалення
листів, резервних копій чи логів: ці строки потрібно налаштувати й виконувати в
поштовій системі та на хостингу. Заявки надсилаються поштою; форма не додає
підписку на маркетингову розсилку. Хеш IP для обмеження запитів слід вважати
псевдонімізованим, а не гарантовано анонімним записом.

Cookie Policy описує `agency_form` та два ключі `sessionStorage`:
`signal:anchor-navigation`, `signal:page-transition`. Вони відповідають поточному коду.
Перевірте cookies, журнали, термін PHP-сесії та обробку даних на обраному хостингу.
Якщо додаються Analytics, Ads, GTM, віджети, зовнішній логотип або нові провайдери,
оновіть відповідні політики. Там, де потрібна згода, додайте її отримання та
блокування відповідних технологій до згоди; сам текст політики цього не забезпечує.

Під час підготовки звірено принципи прозорості та прав користувачів із
[EDPB](https://www.edpb.europa.eu/sme/be-compliant/respect-individuals-rights_en),
[GDPR](https://eur-lex.europa.eu/eli/reg/2016/679/oj/eng), пояснення cookies з
[ICO](https://ico.org.uk/for-organisations/advice-for-small-organisations/privacy-notices-and-cookies/cookies-and-privacy-notices-in-detail/)
і обов’язкові права у договорах із
[Your Europe](https://europa.eu/youreurope/business/selling-in-eu/consumer-contracts-guarantees/consumer-contracts/indexamp_en.htm).
Це джерела перевірки, а не вибір права ЄС або Великої Британії для бізнесу.

## Cookie-банер

Банер працює на всіх восьми публічних сторінках. `js/cookie-preferences.js`
завантажується перед переходами, `js/cookie-banner.js` підключений через main.js,
стилі — `css/cookies.css`. До вибору навігаційне sessionStorage вимкнене.
«Essential only» залишає тільки технічну сесію форми та збереження самого вибору;
«Allow all» дозволяє наявне навігаційне сховище. Аналітика й реклама не встановлені
та не входять до цього дозволу. Кнопка Cookie settings у футері відкриває налаштування.

Вибір записується у localStorage під ключем `signal:cookie-preferences` на 180 днів
(перевірка строку й очищення під час наступного читання). Запис містить версію,
дату вибору й булевий дозвіл navigation. Зміни синхронізуються між вкладками.
Відмова видаляє обидва навігаційні ключі з поточної вкладки; інші відкриті вкладки
також очищають їх після події storage. Якщо браузер блокує збереження,
вибір діє в пам’яті поточної сторінки, про що повідомляється користувачу.

Без дозволу звичайні посилання, форма й плавні якорі в межах сторінки працюють.
Переходи між сторінками не записують ціль у sessionStorage; якір іншої сторінки
відкривається нативно. Системне зменшення руху й загальний перемикач анімацій збережені.

Після додавання нових категорій трекерів потрібно окремо реалізувати їх блокування,
дозволи, відкликання й оновити версію вибору та політики. Наявний банер не блокує
довільні сторонні script-теги автоматично. Рівнозначні кнопки згоди й відмови та
початково вимкнене необов’язкове сховище враховують
[рекомендації ICO](https://ico.org.uk/for-organisations/direct-marketing-and-privacy-and-electronic-communications/guidance-on-the-use-of-storage-and-access-technologies/how-do-we-manage-consent-in-practice/).

## Локальний перегляд

```powershell
php -S 127.0.0.1:8763 -t .
```

Відкрити [сайт](http://127.0.0.1:8763/index.html).

## Система руху

`css/motion.css` містить спільні адаптивні токени й мікровзаємодії;
`js/motion.js` — політику доступності, появу елементів, FAQ та очищення;
`js/motion-scenes.js` — одноразові сценарії пошуку, конверсійних схем,
автоматизації та фінального CTA. Меню, фільтри й форма використовують той самий
модуль через чинні `main.js`, `results.js` і `form.js`.

Поява контенту триває 680–860 мс, великих ілюстрацій — 860–1100 мс. Крива руху
має плавний початок; сусідні картки виходять із кроком 100–140 мс. Картки першого
екрана збираються з різних напрямків із невеликим поворотом і м’яким доведенням
до кінцевої позиції приблизно за 2,6 с. Пошуковий рядок набирається по літерах
із паузами між словами: курсор іде за реальною шириною тексту, двічі блимає під час
паузи для читання, після чого рядок очищується й набирає наступну з шести тематичних
фраз про Google Ads, клієнтів, ліди та аналітику. Перелік — у `js/motion-scenes.js`.
Кожна фраза займає приблизно 4–5 с, цикл працює, доки рядок видимий. Повний текст завжди
залишається в DOM, а без анімації відразу видимий. Іконки отримують
окремий короткий акцент, а конверсійні схеми проходять вузли з кроком 240–320 мс.
На мобільному немає бокового руху й поворотів. Меню, FAQ, кнопки та форма мають
окремі коротші переходи. Поява сцен одноразова; повторюється лише набір пошукового
рядка. Поза екраном і в прихованій вкладці цикл вимикається, при поверненні запускається
знову. Системне зменшення руху вимикає і цей цикл. Перехоплення скролу немає.

Посилання на секції поточної сторінки використовують плавний нативний скрол із
відступом під шапку. Адреси `/` та `/index.html` вважаються тією самою сторінкою,
тому навігація не перезавантажує головну. Якір зберігається в адресі та історії,
фокус переходить до секції; мобільне меню перед переходом закривається.

`js/page-transition.js` завантажується до першого кадру й керує переходом між
сторінками. Нативний View Transition плавно змінює вміст за 640 мс (520 мс на
мобільному). Якщо браузер пропустив або скасував його, запускається резервна
поява вмісту з тією самою тривалістю. Шапка зберігає своє положення. Звичайні посилання
і кнопки браузера «Назад»/«Вперед» залишаються нативними. Для якоря іншої сторінки
`js/anchor-navigation.js` спочатку відкриває її зверху, чекає на сигнал цього ж
контролера про завершення нативної або резервної анімації,
потім плавно прокручує до секції. Одноразова ціль передається через `sessionStorage`,
якір повертається в URL через `replaceState` без зайвого запису в історії. Дії
користувача скасовують відкладене прокручування. Прямі посилання із закладок,
модифіковані кліки та перехід без доступу до сховища зберігають нативну поведінку.
При вимкнених анімаціях або без підтримки анімацій браузер відкриває
сторінку звичайним способом. Додаткові бібліотеки й завантаження HTML через JS не потрібні.

Перемикач — `config/site.json → features.animations`. Системне
`prefers-reduced-motion: reduce` має пріоритет і скасовує активні переходи.
Контент видимий без JS і без підтримки анімацій. Після пізнього завантаження
конфігу або модуля вже видимий перший екран не приховується повторно.

Готові WebP анімуються як цілі композиції. Усередині них іконки, галочки та
стовпчики не є окремими шарами. Незалежні HTML-вузли й короткі SVG-конектори
мають власні послідовності; числові результати не анімуються.

Після впровадження руху в Chrome перевірено:

- 45 комбінацій сторінок і ширин зі зменшеним рухом та 20 з увімкненим;
- швидкі повторні дії меню/FAQ/фільтрів, Escape, фокус і зміну breakpoint;
- помилки форми, повторну спробу й успіх лише після `{ok:true}` у тестовій відповіді;
- вимкнення руху під час переходу, повторну ініціалізацію та очищення стилів;
- повільний конфіг/модуль, роботу без JS/WAAPI і відсутність повторних сцен;
- обробку прихованої вкладки через симуляцію події видимості.

Зовнішні листи під час тестів не надсилались. Safari та фізичні сенсорні пристрої
не перевірялись. Інструменти збірки для деплою не потрібні.

На широких екранах `js/scroll-decor.js` додає кульки з тематичними іконками в бічних
полях головної, починаючи з другої секції. Прокрутка додає нові кульки; гравітація,
відскоки, тертя й зіткнення формують стійкі купки. Кількість обмежена доступним місцем
(до 72), а після зупинки всіх кульок обчислення призупиняються. Ефект ховається в hero,
на екранах вужчих за 1024 px і при вимкнених анімаціях. На ноутбуках контент від другої
секції має відступи для вузьких бічних смуг: кульки менші, а купки обмежені 12–28 елементами
залежно від доступного місця. Фізичний рушій Matter.js 0.20.0
зберігається локально в `js/vendor/matter.js` і завантажується лише при активації ефекту.
Його MIT-ліцензія включена в `js/vendor/LICENSE.matter.txt`; ці файли потрібні для деплою.

Фізичне дно купок залишається нерухомим під час скролу. Над футером піднімається
лише спільний візуальний шар, тому прокрутка не перебудовує стіни й не будить кульки.
Положення нерухомих кульок повторно не записуються в DOM; фізика працює лише доки є
рухомі тіла. Плавне приховування змінює прозорість і трансформацію спільного шару.
У локальному порівнянні Chrome на 1920 × 1080 зі сповільненням CPU у 4 рази,
54 кульками та однаковим 4-секундним скролом біля футера час зайнятості головного
потоку зменшився з 3,73 до 1,19 с, а 95-й перцентиль часу кадру — з 33,4 до 16,8 мс.

Набір кульок містить 34 зображення: 23 тематичні WebP-іконки та 11 PNG-логотипів
продуктів Google в `assets/images/brands/`. Спочатку в перемішаному порядку з'являються
Google Ads, YouTube та Google Analytics, потім інші продукти й іконки без повторів
до завершення набору. Послідовність зберігається при повторній ініціалізації анімацій.
Логотипи завантажені з офіційного CDN Google й використовуються локально. Джерело:
`https://www.gstatic.com/images/branding/product/2x/{product}_96dp.png`.

| Локальний PNG      | Ідентифікатор джерела `{product}` |
| ------------------ | --------------------------------- |
| google-ads         | ads                               |
| youtube-ads        | youtube                           |
| google-analytics   | google_analytics                  |
| google-tag-manager | google_tag_manager                |
| merchant-center    | merchant_center                   |
| looker-studio      | data_studio                       |
| search-console     | search_console                    |
| google-sheets      | sheets_2020q4                     |
| firebase           | firebase                          |
| google-cloud       | google_cloud                      |
| gmail              | gmail_2020q4                      |

Під час ревізії перевірено локальні посилання й ресурси, конфіг, PHP та роботу сторінок на 9 контрольних ширинах. Референси, генератори, залежності розробки, тести й проміжні матеріали прибрано з проєкту.
